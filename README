Installation
------------

1. Get and install the Google App Engine SDK for Python at
   https://developers.google.com/appengine/downloads.
2. Clone SymPy Gamma.
3. From within the Gamma directory, run:

    git submodule init
    git submodule update

4. To start the server, from the directory where the App Engine SDK was
   installed, run:

    ./dev_appserver.py path/to/gamma/directory

Deploy to Google App Engine
---------------------------

    ./appcfg.py update path/to/gamma/directory
